# TCG Comparative - dependencias (todas locales, ninguna es un servicio de IA)
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
PyYAML==6.0.2
APScheduler==3.11.0
openpyxl==3.1.5
jinja2==3.1.5

# Solo para el despliegue con PostgreSQL (Supabase). En local no hace falta:
# sin DATABASE_URL, el proyecto usa SQLite.
psycopg[binary]==3.2.3

# Opcional: solo necesario para tiendas que renderizan productos con JavaScript.
# Instalar con:  pip install playwright  &&  playwright install chromium
# playwright==1.49.1
